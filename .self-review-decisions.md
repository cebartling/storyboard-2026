# Self-review decisions

Findings raised by a self-review pass and deliberately **not** acted on, so future reviews
skip them. Resolved findings are not recorded here — they are in the git history as
`self-review:` commits.

## 2026-08-31 experiment-1-story-mapping (branch)

- **experiments/experiment-1/src/lib/app/use-cases.ts:50** — Every use case is an unguarded
  read-modify-write of the whole aggregate; concurrent requests silently lose data
  - **Severity:** high
  - **Reason for rejection:** Stale finding — optimistic concurrency is already implemented
    and the reviewer's premise ("no version check") is false. `StoryMapRepository.save` is
    documented as saving only when `map.version` is current, and
    `drizzle-story-map-repository.ts` enforces it: the `UPDATE maps` carries
    `WHERE id = ? AND version = ?`, and on `changes === 0` it distinguishes a stale load
    from a deleted map, inside the same transaction. The domain carries `version: number`
    and `schema.maps` has `version integer NOT NULL DEFAULT 0`. A second writer's `save()`
    throws rather than silently winning. The one real residue — that the conflict surfaced
    as a 400 with raw internal version numbers — was fixed under finding 4 (`ConflictError`
    → 409).
  - **Reviewed by:** opus

- **experiments/experiment-1/src/routes/maps/[mapId]/+page.svelte:23** — `handleMove`
  discards the `moveStory` response and has no error path
  - **Severity:** medium
  - **Reason for rejection:** False positive. The code already does exactly what the finding
    proposes: `try`/`catch` around the fetch, `deserialize(await response.text())`,
    `result.type === 'failure'` → a `dragError` rune rendered in the
    `{#if dragError ?? form?.error}` alert, a distinct message for a thrown fetch, and
    `invalidateAll()` in a `finally`. The suggested fix is a description of the existing
    implementation.
  - **Reviewed by:** opus
